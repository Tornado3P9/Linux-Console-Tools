## xclip

```bash
sudo apt install xclip

# show help:
xclip -h

# Copy the textfile content to the clipboard
xclip -selection clipboard < textfile.txt

# Then paste it to where you want to put it
Ctrl+V
```
